const WHATSAPP="YOUR_WHATSAPP_NUMBER";let cart=[];function buy(name,price){cart=[{name,price}];render();document.querySelector("#order").scrollIntoView({behavior:"smooth"})}function custom(){open("https://wa.me/"+WHATSAPP+"?text="+encodeURIComponent("Namaskar Mangalmurti Lights, mala custom LED decoration sathi enquiry karaychi aahe."))}function render(){document.querySelector("#cartCount").textContent=cart.length;let t=cart.reduce((a,x)=>a+x.price,0);document.querySelector("#cart").textContent=cart.length?cart.map(x=>x.name+" - ₹"+x.price.toLocaleString("en-IN")).join(", "):"Cart रिकामी आहे.";document.querySelector("#total").textContent="₹"+t.toLocaleString("en-IN")}function makeOrderId(){return "ML-"+new Date().getTime().toString().slice(-8)}
function submitOrder(e){e.preventDefault();if(!cart.length)return alert("Product निवडा.");let oid=makeOrderId();document.querySelector("#orderId").textContent="Order ID: "+oid;let t=cart.reduce((a,x)=>a+x.price,0);let msg=`NEW ORDER - Mangalmurti Lights\nOrder ID: ${oid}\n\nProduct: ${cart.map(x=>x.name).join(", ")}\nTotal COD: ₹${t}\nCustomer: ${name.value}\nMobile: ${phone.value}\nAddress: ${address.value}, ${city.value} - ${pin.value}\nPayment: Cash on Delivery`;open("https://wa.me/"+WHATSAPP+"?text="+encodeURIComponent(msg))}render();